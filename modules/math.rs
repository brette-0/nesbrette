mod math {
    fn addition(adder : u8*, mut target : u8*, width : u8) -> None;
    fn subtraction(adder : u8*, mut target : u8*, width : u8) -> None;
    fn multiplication(adder : u8*, mut target : u8*, width : u8) -> None;
}