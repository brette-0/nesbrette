#include <stdint.h>

namespace math {
    void addition(uint8_t* adder, uint8_t* target, uint8_t width);
    void subtraction(uint8_t* adder, uint8_t* target, uint8_t width);
    void multiplication(uint8_t* multiplier, uint8_t* target, uint8_t width);
}