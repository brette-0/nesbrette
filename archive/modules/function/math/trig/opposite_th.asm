.proc opposite_th
    ; FUNCTION_MATH_TRIG_OPPOSITE_TH

    hypotenuse = FUNCTION_MATH_TRIG_OPPOSITE_TH_HYPOTENUSE
    theta      = FUNCTION_MATH_TRIG_OPPOSITE_TH_THETA

    


    rts
    .endproc

; calculate opposite from theta and hypotenuse
; h / sin(90) = o / sin(t)
; h = o / sin(t)
; sin(t) * h = o
; o = h * sin(t)